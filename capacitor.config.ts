import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'toumai-frontend_collabo',
  webDir: 'www'
};

export default config;
